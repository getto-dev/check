'use client';
import {useCallback,useEffect,useState} from 'react';
interface BIP extends Event{prompt:()=>Promise<void>;userChoice:Promise<{outcome:'accepted'|'dismissed'}>}
export function usePWA(){const[installPrompt,setInstallPrompt]=useState<BIP|null>(null);const[isInstalled,setInstalled]=useState(false);const[needsUpdate,setNeedsUpdate]=useState(false);const[waiting,setWaiting]=useState<ServiceWorker|null>(null);useEffect(()=>{const standalone=matchMedia('(display-mode: standalone)').matches||Boolean((navigator as Navigator&{standalone?:boolean}).standalone);// eslint-disable-next-line react-hooks/set-state-in-effect
setInstalled(standalone);const before=(e:Event)=>{e.preventDefault();setInstallPrompt(e as BIP)};window.addEventListener('beforeinstallprompt',before);const installed=()=>{setInstalled(true);setInstallPrompt(null)};window.addEventListener('appinstalled',installed);if('serviceWorker'in navigator){navigator.serviceWorker.getRegistration().then(r=>{if(r?.waiting){setWaiting(r.waiting);setNeedsUpdate(true)}r?.addEventListener('updatefound',()=>{const w=r.installing;if(!w)return;w.addEventListener('statechange',()=>{if(w.state==='installed'&&navigator.serviceWorker.controller){setWaiting(w);setNeedsUpdate(true)}})})})}return()=>{window.removeEventListener('beforeinstallprompt',before);window.removeEventListener('appinstalled',installed)}},[]);const install=useCallback(async()=>{if(!installPrompt)return false;await installPrompt.prompt();const r=await installPrompt.userChoice;return r.outcome==='accepted'},[installPrompt]);const checkForUpdates=useCallback(async()=>{if(!('serviceWorker'in navigator)||!navigator.onLine)return false;const r=await navigator.serviceWorker.getRegistration();if(!r)return false;await r.update();if(r.waiting){setWaiting(r.waiting);setNeedsUpdate(true);return true}return false},[]);const applyUpdate=useCallback(()=>{if(waiting){waiting.postMessage('skipWaiting');window.location.reload()}},[waiting]);return{canInstall:Boolean(installPrompt),install,isInstalled,isStandalone:isInstalled,needsUpdate,waitingWorker:waiting,checkForUpdates,applyUpdate}}
