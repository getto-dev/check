'use client';
import { useMemo } from 'react'; import { Header } from '@/components/Header'; import { SearchSection } from '@/components/SearchSection'; import { CatalogList } from '@/components/CatalogList'; import { InvoiceSection } from '@/components/InvoiceSection'; import { ManualSection } from '@/components/ManualSection'; import { SettingsSection } from '@/components/SettingsSection'; import { AddItemModal } from '@/components/AddItemModal'; import { InstallBanner } from '@/components/InstallBanner'; import { IOSInstallBanner } from '@/components/IOSInstallBanner'; import { useAppStore } from '@/lib/store'; import { usePWA } from '@/hooks/use-pwa';
export default function HomePage(){const{currentTab,setTab}=useAppStore();const pwa=usePWA();const content=useMemo(()=>{switch(currentTab){case'invoice':return <InvoiceSection/>;case'manual':return <ManualSection/>;case'settings':return <SettingsSection/>;default:return <><SearchSection onManualClick={()=>setTab('manual')}/><section className="flex-1 px-3 sm:px-4 pb-6 mx-auto w-full max-w-5xl overflow-y-auto"><CatalogList/></section></>}},[currentTab,setTab]);return <div className="min-h-screen flex flex-col bg-background"><Header/><main className="flex-1 flex flex-col mx-auto w-full max-w-5xl">{content}</main><AddItemModal/><InstallBanner onInstall={pwa.install} canInstall={pwa.canInstall} isInstalled={pwa.isInstalled}/><IOSInstallBanner isStandalone={pwa.isStandalone} isInstalled={pwa.isInstalled}/></div>}
