'use client';
import { useRef,useState } from 'react'; import { FolderOpen,Save,Printer,Minus,Plus,Trash2,RotateCcw } from 'lucide-react'; import { useAppStore } from '@/lib/store'; import { formatCurrency,formatQuantity } from '@/lib/format'; import { loadEstimateFromFile,saveEstimateToFile,EstimateFileError } from '@/lib/estimate-format'; import { ConfirmDialog } from '@/components/ui/confirm-dialog'; import { useToast } from '@/components/ui/toast';
export function InvoiceSection(){const{items,settings,updateQuantity,removeItem,clearItems,loadEstimateData}=useAppStore();const totals=useAppStore(s=>s.calculateTotals());const file=useRef<HTMLInputElement>(null);const[overwrite,setOverwrite]=useState<File|null>(null);const{showToast}=useToast();const load=async(f:File)=>{try{const d=await loadEstimateFromFile(f);loadEstimateData(d.items,d.settings);showToast(`Смета «${d.name}» открыта`,'success')}catch(e){showToast(e instanceof EstimateFileError?e.message:'Не удалось открыть файл','error')}};const open=(f:File)=>{if(items.length){setOverwrite(f)}else void load(f)};return <div className="space-y-5"><header className="flex flex-wrap items-center justify-between gap-3 pb-4 border-b-2 border-primary"><div><h2 className="text-2xl font-extrabold gradient-text">Смета</h2><p className="text-xs text-muted-foreground">{settings.address||'Адрес не указан'}</p></div><div className="flex gap-2"><button className="button" onClick={()=>file.current?.click()}><FolderOpen className="w-4 h-4"/>Открыть</button><button className="button" onClick={()=>saveEstimateToFile(items,settings)} disabled={!items.length}><Save className="w-4 h-4"/>Сохранить</button><button className="button" onClick={()=>window.print()} disabled={!items.length}><Printer className="w-4 h-4"/>PDF</button><input ref={file} type="file" accept=".html,text/html" hidden onChange={e=>{const f=e.target.files?.[0];if(f)open(f);e.target.value=''}}/></div></header>{items.length===0?<div className="text-center py-16 text-muted-foreground"><p className="text-lg font-bold text-foreground">Смета пуста</p><p>Добавьте позиции из каталога.</p></div>:<>{items.map(i=><div key={i.id} className="flex items-center gap-2 border-b border-border py-3"><div className="flex-1 min-w-0"><div className="font-bold truncate">{i.name}</div><div className="text-xs text-muted-foreground">{formatCurrency(i.priceKopecks)} / {i.unit}</div></div><div className="flex items-center gap-1"><button className="icon-button" onClick={()=>updateQuantity(i.id,i.quantity-1)}><Minus size={15}/></button><span className="min-w-8 text-center font-bold">{formatQuantity(i.quantity)}</span><button className="icon-button" onClick={()=>updateQuantity(i.id,i.quantity+1)}><Plus size={15}/></button></div><div className="w-28 text-right font-extrabold">{formatCurrency(Math.round(i.priceKopecks*i.quantity))}</div><button className="icon-button" onClick={()=>removeItem(i.id)} aria-label="Удалить"><Trash2 size={16}/></button></div>)}<div className="rounded-2xl bg-card border border-border p-4 space-y-2"><div className="total-line"><span>Работы</span><span>{formatCurrency(totals.servicesKopecks)}</span></div><div className="total-line"><span>Материалы</span><span>{formatCurrency(totals.productsKopecks)}</span></div>{totals.discountKopecks>0&&<div className="total-line"><span>Скидка {settings.discount}%</span><span>−{formatCurrency(totals.discountKopecks)}</span></div>}<div className="total-line grand"><span>Итого</span><span>{formatCurrency(totals.grandTotalKopecks)}</span></div><button className="button" onClick={()=>clearItems()}><RotateCcw className="w-4 h-4"/>Очистить</button></div></>}<ConfirmDialog open={overwrite!==null} onOpenChange={o=>!o&&setOverwrite(null)} title="Заменить текущую смету?" description="Позиции текущей сметы будут заменены данными из файла." confirmText="Открыть" onConfirm={()=>{if(overwrite)void load(overwrite);setOverwrite(null)}} variant="destructive"/></div>}
