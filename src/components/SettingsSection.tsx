'use client';
import { memo,useEffect,useCallback,useState } from 'react';
import { useAppStore } from '@/lib/store'; import { ThemeMode } from '@/lib/types'; import { APP_VERSION } from '@/lib/constants'; import { cn } from '@/lib/utils'; import { Sun,Moon,Monitor,Download,RefreshCw,MessageCircle } from 'lucide-react'; import { useTheme } from 'next-themes'; import { usePWA } from '@/hooks/use-pwa'; import { Button } from '@/components/ui/button'; import { useToast } from '@/components/ui/toast';
export const SettingsSection=memo(function SettingsSection(){const{settings,updateSettings,themeMode,setThemeMode}=useAppStore();const{setTheme}=useTheme();const{isInstalled,canInstall,needsUpdate,install,checkForUpdates,applyUpdate}=usePWA();const{showToast}=useToast();const[isChecking,setIsChecking]=useState(false);useEffect(()=>setTheme(themeMode),[themeMode,setTheme]);const check=useCallback(async()=>{setIsChecking(true);const u=await checkForUpdates();setIsChecking(false);if(!u)showToast('Обновлений нет','info')},[checkForUpdates,showToast]);const installApp=useCallback(async()=>{if(!canInstall){showToast('Используйте Chrome на Android или Safari на iOS','info');return;}await install()},[canInstall,install,showToast]);return <div className="space-y-5 sm:space-y-6"><h2 className="text-lg sm:text-xl font-extrabold pb-3 border-b border-border">Настройки</h2><div className="space-y-2"><label htmlFor="address" className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground block">Адрес объекта</label><input id="address" type="text" value={settings.address} onChange={e=>updateSettings({address:e.target.value})} placeholder="г. Москва, ул. Строителей, д. 10" className="w-full px-4 py-3.5 rounded-xl text-sm bg-card border-2 border-border focus:outline-none focus:border-primary"/></div><div className="space-y-2"><label htmlFor="discount" className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground block">Скидка на услуги</label><div className="bg-muted rounded-xl p-4"><input id="discount" type="range" value={settings.discount} onChange={e=>updateSettings({discount:Math.max(0,Math.min(50,Number(e.target.value)))})} min={0} max={50} step={5} className="w-full"/><div className="flex justify-between mt-2.5 text-xs font-bold text-muted-foreground"><span>0%</span><span className="text-lg gradient-text">{settings.discount}%</span><span>50%</span></div></div></div><div className="space-y-2"><span className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground block">Тема</span><div className="grid grid-cols-3 gap-2">{([['light',Sun,'Светлая'],['dark',Moon,'Тёмная'],['system',Monitor,'Система']] as const).map(([m,Icon,label])=><button key={m} onClick={()=>setThemeMode(m as ThemeMode)} className={cn('p-4 rounded-xl text-center border-2',themeMode===m?'border-primary bg-primary/10':'border-border bg-card')} aria-pressed={themeMode===m}><Icon className="w-6 h-6 mx-auto mb-2"/><span className="text-[11px] font-bold">{label}</span></button>)}</div></div><div className="space-y-2"><span className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground block">Приложение</span><div className="flex flex-col gap-2.5"><Button variant="outline" onClick={needsUpdate?applyUpdate:check} disabled={isChecking} className="w-full justify-start gap-3 py-3.5 rounded-xl"><RefreshCw className={cn('w-5 h-5',isChecking&&'animate-spin')}/>{isChecking?'Проверка...':needsUpdate?'Применить обновление':'Проверить обновления'}</Button>{!isInstalled?<Button variant="outline" onClick={installApp} className={cn('w-full justify-start gap-3 py-3.5 rounded-xl',canInstall&&'gradient-bg text-white')}><Download className="w-5 h-5"/>{canInstall?'Установить приложение':'Как установить приложение'}</Button>:<div className="text-xs text-center text-muted-foreground py-2">✓ Приложение установлено</div>}</div></div><a href="tg://resolve?domain=gettocode" target="_blank" rel="noopener noreferrer" className="flex items-center gap-3 w-full py-3.5 px-4 rounded-xl text-sm font-bold border-2 border-border bg-card"><MessageCircle className="w-5 h-5 text-primary"/>Telegram</a><footer className="text-center pt-6 text-[11px] text-muted-foreground/60">Getto-Dev v{APP_VERSION} • 2026</footer></div>});
