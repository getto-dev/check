/* eslint-disable react-hooks/set-state-in-effect */
'use client';
import { memo, useState, useCallback, useEffect } from 'react';
import { useAppStore, haptic } from '@/lib/store';
import { cn } from '@/lib/utils'; import { Button } from '@/components/ui/button';
const FormInput=memo(function FormInput({label,value,onChange,placeholder,className,id}:{label:string;value:string;onChange:(value:string)=>void;placeholder?:string;className?:string;id?:string}){const inputId=id||label.toLowerCase().replace(/\s+/g,'-');return <div className={cn('space-y-2',className)}><label htmlFor={inputId} className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground block">{label}</label><input id={inputId} type="text" value={value} onChange={e=>onChange(e.target.value)} placeholder={placeholder} className="w-full px-4 py-3.5 rounded-xl bg-card border-2 border-border focus:outline-none focus:border-primary"/></div>});
const NumberInput=memo(function NumberInput({label,value,onChange,min}:{label:string;value:number;onChange:(value:number)=>void;min?:number}){const[displayValue,setDisplayValue]=useState(String(value));useEffect(()=>setDisplayValue(String(value)),[value]);return <div className="space-y-2"><label className="text-[11px] font-bold uppercase tracking-widest text-muted-foreground block">{label}</label><input type="number" inputMode="decimal" step="any" value={displayValue} min={min} onChange={e=>{setDisplayValue(e.target.value);const n=Number(e.target.value);if(Number.isFinite(n))onChange(n)}} onBlur={()=>{const n=Number(displayValue);const v=Number.isFinite(n)?n:(min??0);onChange(v);setDisplayValue(String(v))}} className="w-full px-4 py-3.5 rounded-xl bg-card border-2 border-border font-bold"/></div>});
export function ManualSection(){const{manualType,setManualType,addManualItem}=useAppStore();const[name,setName]=useState('');const[description,setDescription]=useState('');const[quantity,setQuantity]=useState(1);const[unit,setUnit]=useState('шт');const[price,setPrice]=useState(0);const handleAdd=useCallback(()=>{if(!name.trim())return;addManualItem({name:name.trim(),description:description.trim(),quantity:Math.max(.1,quantity),unit:unit||'шт',priceKopecks:Math.max(0,Math.round(price*100)),type:manualType,categoryId:'manual'});setName('');setDescription('');setQuantity(1);setUnit('шт');setPrice(0);haptic('success')},[name,description,quantity,unit,price,manualType,addManualItem]);return <div className="space-y-5"><div className="flex bg-muted rounded-xl p-1" role="tablist"><button onClick={()=>setManualType('service')} className={cn('flex-1 py-3 rounded-lg font-bold',manualType==='service'?'bg-card text-primary shadow':'text-muted-foreground')}>Услуга</button><button onClick={()=>setManualType('product')} className={cn('flex-1 py-3 rounded-lg font-bold',manualType==='product'?'bg-card text-primary shadow':'text-muted-foreground')}>Товар</button></div><FormInput label="Название" value={name} onChange={setName} placeholder={manualType==='service'?'Установка крана...':'Труба PPR 20мм...'}/><FormInput label="Описание" value={description} onChange={setDescription} placeholder="Детали..."/><div className="grid grid-cols-3 gap-3"><NumberInput label="Кол-во" value={quantity} onChange={setQuantity} min={1}/><FormInput label="Ед.изм" value={unit} onChange={setUnit}/><NumberInput label="Цена ₽" value={price} onChange={setPrice} min={0}/></div><Button onClick={handleAdd} disabled={!name.trim()} className="w-full py-4 rounded-xl gradient-bg text-white font-extrabold">Добавить в смету</Button></div>}
